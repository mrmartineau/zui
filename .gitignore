node_modules
dist
.DS_Store
demo/node_modules
demo/dist
demo/.astro
demo/.wrangler

.wrangler

# Generated by raycast-zui `pnpm run generate:data` — kept out of this repo, but
# MUST NOT be ignored by a .gitignore inside packages/raycast-zui: `ray publish`
# copies that directory (including any .gitignore) into the raycast/extensions
# fork and runs plain `git add`, which would silently drop the snapshot there.
packages/raycast-zui/src/data/reference.json
