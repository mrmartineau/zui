{
  "name": "zui-workspace",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "^2.4.12"
  },
  "scripts": {
    "build": "pnpm --filter @mrmartineau/zui run build",
    "check": "biome check --write",
    "dev": "pnpm --filter @mrmartineau/zui run dev",
    "docs:build": "pnpm --filter @mrmartineau/zui run build && pnpm --filter docs run build",
    "docs:deploy": "pnpm run docs:build && pnpm --filter docs run deploy",
    "docs:dev": "pnpm --filter @mrmartineau/zui run build && pnpm --filter docs run dev",
    "generate:colors": "pnpm --filter @mrmartineau/zui run generate:colors",
    "watch": "pnpm --filter @mrmartineau/zui run watch"
  }
}
