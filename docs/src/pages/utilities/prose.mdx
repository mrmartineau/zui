---
layout: ../../layouts/Layout.astro
title: Prose
description: Rich text styling for long-form content — typography, spacing, and element styles in one class.
---

import { TokenGrid } from '@mrmartineau/zui-theme/astro'
import { TokenRow } from '@mrmartineau/zui-theme/astro'
import { CopyCode } from '@mrmartineau/zui-theme/astro'
import { Demo } from '@mrmartineau/zui-theme/astro'

## Usage

Wrap any block of HTML content with `.prose` to get styled headings, paragraphs, lists, blockquotes, links, code blocks, and media.

<Demo
  html={`<article class="prose">
  <h1>Article Title</h1>
  <p>Body text with <a href="#">links</a> and <code>inline code</code>.</p>
  <ul>
    <li>List items</li>
    <li>Are styled automatically</li>
  </ul>
  <blockquote>Blockquotes get a left border.</blockquote>
</article>`}
  react={`import { Prose } from '@mrmartineau/zui/react'

<Prose>
  <h1>Article Title</h1>
  <p>Body text with <a href="#">links</a> and <code>inline code</code>.</p>
  <ul>
    <li>List items</li>
    <li>Are styled automatically</li>
  </ul>
  <blockquote>Blockquotes get a left border.</blockquote>
</Prose>`}
  astro={`---
import { Prose } from '@mrmartineau/zui/astro'
---

<Prose>
  <h1>Article Title</h1>
  <p>Body text with <a href="#">links</a> and <code>inline code</code>.</p>
  <ul>
    <li>List items</li>
    <li>Are styled automatically</li>
  </ul>
  <blockquote>Blockquotes get a left border.</blockquote>
</Prose>`}
  solid={`import { Prose } from '@mrmartineau/zui/solid'

<Prose>
  <h1>Article Title</h1>
  <p>Body text with <a href="#">links</a> and <code>inline code</code>.</p>
  <ul>
    <li>List items</li>
    <li>Are styled automatically</li>
  </ul>
  <blockquote>Blockquotes get a left border.</blockquote>
</Prose>`}
  vue={`<template>
  <Prose>
    <h1>Article Title</h1>
    <p>Body text with <a href="#">links</a> and <code>inline code</code>.</p>
    <ul>
      <li>List items</li>
      <li>Are styled automatically</li>
    </ul>
    <blockquote>Blockquotes get a left border.</blockquote>
  </Prose>
</template>

<script setup>
import { Prose } from '@mrmartineau/zui/vue'
</script>`}
>
  <article class="prose">
    <h1>Article Title</h1>
    <p>Body text with <a href="#">links</a> and <code>inline code</code>.</p>
    <ul>
      <li>List items</li>
      <li>Are styled automatically</li>
    </ul>
    <blockquote>Blockquotes get a left border.</blockquote>
  </article>
</Demo>

## Kitchen sink

<Demo
  html={`<article class="prose">
    <h1>Heading 1</h1>
    <p>Paragraph text with <a href="#">a link</a>, <strong>bold</strong>, <em>italic</em>, and <code>inline code</code>.</p>
    <h2>Heading 2</h2>
    <p>Another paragraph to show flow spacing between elements. Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p>
    <h3>Heading 3</h3>
    <h4>Heading 4</h4>
    <ul>
      <li>Unordered list item one</li>
      <li>Item two
        <ul>
          <li>Nested item</li>
          <li>Another nested item
            <ul>
              <li>Deeply nested</li>
            </ul>
          </li>
        </ul>
      </li>
      <li>Item three</li>
    </ul>
    <ol>
      <li>Ordered list item one</li>
      <li>Item two
        <ol>
          <li>Nested ordered</li>
          <li>Another nested
            <ol>
              <li>Deeply nested</li>
            </ol>
          </li>
        </ol>
      </li>
      <li>Item three</li>
    </ol>
    <blockquote>A blockquote for emphasis or attribution. Styled with a left border and italic text.</blockquote>
    <pre><code>// Code block
  const greeting = 'Hello, world!'
  console.log(greeting)</code></pre>
    <hr />
    <p>Content after a horizontal rule.</p>

  </article>`}
  react={`import { Prose } from '@mrmartineau/zui/react'

<Prose>
  <h1>Heading 1</h1>
  <p>Paragraph text with <a href="#">a link</a>, <strong>bold</strong>, <em>italic</em>, and <code>inline code</code>.</p>
  <h2>Heading 2</h2>
  <p>Another paragraph to show flow spacing.</p>
  <h3>Heading 3</h3>
  <h4>Heading 4</h4>
  <ul>
    <li>Unordered list item</li>
    <li>Item two<ul><li>Nested</li></ul></li>
  </ul>
  <ol>
    <li>Ordered list item</li>
    <li>Item two<ol><li>Nested</li></ol></li>
  </ol>
  <blockquote>A blockquote for emphasis.</blockquote>
  <pre><code>{'const greeting = "Hello, world!"'}</code></pre>
  <hr />
  <p>Content after a horizontal rule.</p>
</Prose>`}
  astro={`---
import { Prose } from '@mrmartineau/zui/astro'
---

<Prose>
  <h1>Heading 1</h1>
  <p>Paragraph text with <a href="#">a link</a>, <strong>bold</strong>, <em>italic</em>, and <code>inline code</code>.</p>
  <h2>Heading 2</h2>
  <p>Another paragraph to show flow spacing.</p>
  <h3>Heading 3</h3>
  <h4>Heading 4</h4>
  <ul>
    <li>Unordered list item</li>
    <li>Item two<ul><li>Nested</li></ul></li>
  </ul>
  <ol>
    <li>Ordered list item</li>
    <li>Item two<ol><li>Nested</li></ol></li>
  </ol>
  <blockquote>A blockquote for emphasis.</blockquote>
  <pre><code>const greeting = 'Hello, world!'</code></pre>
  <hr />
  <p>Content after a horizontal rule.</p>
</Prose>`}
  solid={`import { Prose } from '@mrmartineau/zui/solid'

<Prose>
  <h1>Heading 1</h1>
  <p>Paragraph text with <a href="#">a link</a>, <strong>bold</strong>, <em>italic</em>, and <code>inline code</code>.</p>
  <h2>Heading 2</h2>
  <p>Another paragraph to show flow spacing.</p>
  <h3>Heading 3</h3>
  <h4>Heading 4</h4>
  <ul>
    <li>Unordered list item</li>
    <li>Item two<ul><li>Nested</li></ul></li>
  </ul>
  <ol>
    <li>Ordered list item</li>
    <li>Item two<ol><li>Nested</li></ol></li>
  </ol>
  <blockquote>A blockquote for emphasis.</blockquote>
  <pre><code>{'const greeting = "Hello, world!"'}</code></pre>
  <hr />
  <p>Content after a horizontal rule.</p>
</Prose>`}
  vue={`<template>
  <Prose>
    <h1>Heading 1</h1>
    <p>Paragraph text with <a href="#">a link</a>, <strong>bold</strong>, <em>italic</em>, and <code>inline code</code>.</p>
    <h2>Heading 2</h2>
    <p>Another paragraph to show flow spacing.</p>
    <h3>Heading 3</h3>
    <h4>Heading 4</h4>
    <ul>
      <li>Unordered list item</li>
      <li>Item two<ul><li>Nested</li></ul></li>
    </ul>
    <ol>
      <li>Ordered list item</li>
      <li>Item two<ol><li>Nested</li></ol></li>
    </ol>
    <blockquote>A blockquote for emphasis.</blockquote>
    <pre><code>const greeting = 'Hello, world!'</code></pre>
    <hr />
    <p>Content after a horizontal rule.</p>
  </Prose>
</template>

<script setup>
import { Prose } from '@mrmartineau/zui/vue'
{'</script>'}`}
>
  <article class="prose"><h1>Heading 1</h1><p>Paragraph text with <a href="#">a link</a>, <strong>bold</strong>, <em>italic</em>, and <code>inline code</code>.</p><h2>Heading 2</h2><p>Another paragraph to show flow spacing between elements. Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p><h3>Heading 3</h3><h4>Heading 4</h4><ul><li>Unordered list item one</li><li>Item two<ul><li>Nested item</li><li>Another nested item<ul><li>Deeply nested</li></ul></li></ul></li><li>Item three</li></ul><ol><li>Ordered list item one</li><li>Item two<ol><li>Nested ordered</li><li>Another nested<ol><li>Deeply nested</li></ol></li></ol></li><li>Item three</li></ol><blockquote>A blockquote for emphasis or attribution. Styled with a left border and italic text.</blockquote><pre><code>{"// Code block\nconst greeting = 'Hello, world!'\nconsole.log(greeting)"}</code></pre><hr /><p>Content after a horizontal rule.</p><img src="https://placehold.co/600x400" alt="Placeholder image" /><iframe width="560" height="315" src="https://www.youtube-nocookie.com/embed/_v4CpSsHpZQ?si=GRdd906T5TeX2D3g" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe></article>
</Demo>

## Inverted

Add `.inverted` when prose sits on a dark surface — such as one filled with `--color-text`. It swaps the text/background colour mapping so links, code, and body copy stay readable.

<Demo
  html={`<div style="background: var(--color-text); padding: var(--space-md); border-radius: var(--radius-lg);">
  <article class="prose inverted">
    <h2>Inverted prose</h2>
    <p>Body text with <a href="#">links</a> and <code>inline code</code> on a dark surface.</p>
    <blockquote>Blockquotes stay legible too.</blockquote>
  </article>
</div>`}
  react={`import { Prose } from '@mrmartineau/zui/react'

<div style={{ background: 'var(--color-text)', padding: 'var(--space-md)', borderRadius: 'var(--radius-lg)' }}>
  <Prose className="inverted">
    <h2>Inverted prose</h2>
    <p>Body text with <a href="#">links</a> and <code>inline code</code> on a dark surface.</p>
    <blockquote>Blockquotes stay legible too.</blockquote>
  </Prose>
</div>`}
  astro={`---
import { Prose } from '@mrmartineau/zui/astro'
---

<div style="background: var(--color-text); padding: var(--space-md); border-radius: var(--radius-lg);">
  <Prose class="inverted">
    <h2>Inverted prose</h2>
    <p>Body text with <a href="#">links</a> and <code>inline code</code> on a dark surface.</p>
    <blockquote>Blockquotes stay legible too.</blockquote>
  </Prose>
</div>`}
  solid={`import { Prose } from '@mrmartineau/zui/solid'

<div style={{ background: 'var(--color-text)', padding: 'var(--space-md)', 'border-radius': 'var(--radius-lg)' }}>
  <Prose class="inverted">
    <h2>Inverted prose</h2>
    <p>Body text with <a href="#">links</a> and <code>inline code</code> on a dark surface.</p>
    <blockquote>Blockquotes stay legible too.</blockquote>
  </Prose>
</div>`}
  vue={`<template>
  <div style="background: var(--color-text); padding: var(--space-md); border-radius: var(--radius-lg);">
    <Prose class="inverted">
      <h2>Inverted prose</h2>
      <p>Body text with <a href="#">links</a> and <code>inline code</code> on a dark surface.</p>
      <blockquote>Blockquotes stay legible too.</blockquote>
    </Prose>
  </div>
</template>

<script setup>
import { Prose } from '@mrmartineau/zui/vue'
</script>`}
>
  <div style="background: var(--color-text); padding: var(--space-md); border-radius: var(--radius-lg); flex: 1;">
    <article class="prose inverted">
      <h2>Inverted prose</h2>
      <p>Body text with <a href="#">links</a> and <code>inline code</code> on a dark surface.</p>
      <blockquote>Blockquotes stay legible too.</blockquote>
    </article>
  </div>
</Demo>

## What it styles

- **Headings** — h1–h4 with type scale, heading font family, and balanced text wrapping
- **Flow spacing** — automatic vertical rhythm via `--flow-space`
- **Links** — accent-coloured with underline decoration and hover transitions
- **Lists** — nested list style types (disc → circle → square for ul; decimal → lower-roman → lower-alpha for ol)
- **Blockquotes** — left border with italic styling
- **Code** — inline `code` / `pre` blocks with monospace font and background
- **Media** — images, iframes, and videos get border-radius and a subtle border
- **Horizontal rules** — styled as a thin background-colour line

## Overriding styles

All prose styles are wrapped in `:where(.prose)`, giving them zero specificity. Any single class — a ZUI utility like `.mt-lg` or `.zui-text-2`, or one of your own — overrides the prose defaults on that element, no `!important` needed. Prose still overrides component styles (`zui.components`) because layer order takes precedence over specificity.

```html
<article class="prose">
  <h2>Default prose heading</h2>
  <h2 class="zui-text-6 mt-2xl">Bigger, with extra space above</h2>
</article>
```

## Custom properties

```css
.prose {
  --prose-accent-color: var(--color-theme);
  --prose-link-color: var(--color-text);
  --prose-link-color-decoration: oklch(from var(--color-text) l c h / 50%);
  --prose-link-color-decoration-hover: oklch(from var(--color-text) l c h / 90%);
  --prose-code-color: oklch(from var(--color-text) calc(l + 0.1) c h);
  --prose-code-bg-color: oklch(from var(--color-background) calc(l + 0.1) c h);
  --prose-scroll-margin: 1.5rem;
}
```

## Modifiers

<TokenGrid>
  <TokenRow><CopyCode>.prose.inverted</CopyCode><span>Inverted colour scheme for dark backgrounds</span></TokenRow>
  <TokenRow><CopyCode>.prose.max-width-p</CopyCode><span>Limits paragraph width to 65ch for readability</span></TokenRow>
</TokenGrid>

