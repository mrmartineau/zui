---
layout: ../../layouts/Layout.astro
title: Textarea
---

import Demo from '../../components/Demo.astro'

# Textarea

A multi-line text input with auto-sizing via `field-sizing: content`.

## Usage

<Demo
  html={`<textarea class="zui-textarea" placeholder="Write something..."></textarea>`}
  react={`import { Textarea } from '@mrmartineau/zui/react'

<Textarea placeholder="Write something..." />`}
  astro={`---
import { Textarea } from '@mrmartineau/zui/astro'
---

<Textarea placeholder="Write something..." />`}
  solid={`import { Textarea } from '@mrmartineau/zui/solid'

<Textarea placeholder="Write something..." />`}
  vue={`<template>
  <Textarea placeholder="Write something..." />
</template>

<script setup>
import { Textarea } from '@mrmartineau/zui/vue'
</script>`}
>
  <textarea class="zui-textarea" placeholder="Write something..."></textarea>
</Demo>

## With Label

<Demo
  html={`<label class="zui-label" for="message">Message</label>
<textarea class="zui-textarea" id="message" placeholder="Your message..."></textarea>`}
  react={`import { Label, Textarea } from '@mrmartineau/zui/react'

<Label htmlFor="message">Message</Label>
<Textarea id="message" placeholder="Your message..." />`}
  astro={`---
import { Label, Textarea } from '@mrmartineau/zui/astro'
---

<Label for="message">Message</Label>
<Textarea id="message" placeholder="Your message..." />`}
  solid={`import { Label, Textarea } from '@mrmartineau/zui/solid'

<Label htmlFor="message">Message</Label>
<Textarea id="message" placeholder="Your message..." />`}
  vue={`<template>
  <Label for="message">Message</Label>
<Textarea id="message" placeholder="Your message..." />
</template>

<script setup>
import { Label, Textarea } from '@mrmartineau/zui/vue'
</script>`}
>
  <div style="width: 100%;">
    <label class="zui-label" for="message">Message</label>
    <textarea class="zui-textarea" id="message" placeholder="Your message..."></textarea>
  </div>
</Demo>

## Invalid State

<Demo
  html={`<textarea class="zui-textarea" aria-invalid="true">This field has an error</textarea>`}
  react={`import { Textarea } from '@mrmartineau/zui/react'

<Textarea aria-invalid="true" defaultValue="This field has an error" />`}
  astro={`---
import { Textarea } from '@mrmartineau/zui/astro'
---

<Textarea aria-invalid="true">This field has an error</Textarea>`}
  solid={`import { Textarea } from '@mrmartineau/zui/solid'

<Textarea aria-invalid="true" defaultValue="This field has an error" />`}
  vue={`<template>
  <Textarea aria-invalid="true" value="This field has an error" />
</template>

<script setup>
import { Textarea } from '@mrmartineau/zui/vue'
</script>`}
>
  <textarea class="zui-textarea" aria-invalid="true">This field has an error</textarea>
</Demo>

## Disabled

<Demo
  html={`<textarea class="zui-textarea" disabled>Cannot edit this</textarea>`}
  react={`import { Textarea } from '@mrmartineau/zui/react'

<Textarea disabled defaultValue="Cannot edit this" />`}
  astro={`---
import { Textarea } from '@mrmartineau/zui/astro'
---

<Textarea disabled>Cannot edit this</Textarea>`}
  solid={`import { Textarea } from '@mrmartineau/zui/solid'

<Textarea disabled defaultValue="Cannot edit this" />`}
  vue={`<template>
  <Textarea disabled value="Cannot edit this" />
</template>

<script setup>
import { Textarea } from '@mrmartineau/zui/vue'
</script>`}
>
  <textarea class="zui-textarea" disabled>Cannot edit this</textarea>
</Demo>

## CSS custom properties

| Property | Default | Description |
|---|---|---|
| `--zui-textarea-radius` | `var(--radius-lg)` | Border radius |
| `--zui-textarea-border` | `var(--color-border)` | Border colour |
| `--zui-textarea-bg` | `var(--color-background)` | Background colour |
| `--zui-textarea-fg` | `var(--color-text)` | Text colour |
| `--zui-textarea-padding` | `var(--space-xs)` | Padding inside the textarea |
